fragment Template on Template {
  id
  agent {
    registry {
      name
      app {
        version
        identifier
      }
      user {
        username
      }
    }
  }
  node {
    ...Node
  }
  params
}
