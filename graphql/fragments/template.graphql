fragment Template on Template {
  id
  interface
  agent {
    registry {
      name
      app {
        version
        identifier
      }
      user {
        username
      }
    }
  }
  node {
    ...Node
  }
  extensions
  params
}
