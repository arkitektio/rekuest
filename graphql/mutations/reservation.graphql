mutation reserve(
  $node: ID!
  $template: ID
  $params: ReserveParamsInput
  $title: String
  $imitate: ID
  $appGroup: ID
  $reference: String
  $provision: ID
  $binds: ReserveBindsInput
) {
  reserve(
    node: $node
    template: $template
    params: $params
    title: $title
    imitate: $imitate
    provision: $provision
    appGroup: $appGroup
    binds: $binds
    reference: $reference
  ) {
    ...Reservation
  }
}

mutation unreserve($id: ID!) {
  unreserve(id: $id) {
    ...Reservation
  }
}
