""" This file was code generated by turms. If you want to change the contents of this file, you should make sure to add the MergeProcessor to your config will keep your changes when you re-run turms)."""
    
from datetime import datetime
from enum import Enum
from pydantic import AliasChoices, BaseModel, ConfigDict, Field
from rath.scalars import ID
from rekuest.scalars import ActionHash, Args, Identifier, JSONSerializable, MediaLike, SearchQuery
from rekuest.traits.agent import ImplementAgentInputTrait
from rekuest.traits.blok import BlokImplementationInputTrait, CreateBlokInputTrait
from rekuest.traits.implementation import ImplementationInputTrait
from rekuest.traits.ports import DefinitionInputTrait, EffectInputTrait, PortTrait, SearchWidgetInputTrait, SliderWidgetInputTrait, StateChoiceWidgetInputTrait, ValidatorInputTrait
from typing import Annotated, Any, Literal

class GraphQLDefault:
    """Records a GraphQL field schema default value. The client omits the field so the server applies its own default; this preserves the value for introspection."""

    def __init__(self, value):
        self.value = value

    def __repr__(self):
        return 'GraphQLDefault(' + repr(self.value) + ')'

class ActionKind(str, Enum):
    """The kind of action."""
    FUNCTION = 'FUNCTION'
    GENERATOR = 'GENERATOR'
    __str__ = str.__str__

class AgentKind(str, Enum):
    """No documentation"""
    WEBSOCKET = 'WEBSOCKET'
    WEBHOOK = 'WEBHOOK'
    __str__ = str.__str__

class AssignPolicy(str, Enum):
    """No documentation"""
    AUTOMATIC = 'AUTOMATIC'
    BALANCED = 'BALANCED'
    ROUND_ROBIN = 'ROUND_ROBIN'
    LEAST_BUSY = 'LEAST_BUSY'
    FASTEST_RESPONSE = 'FASTEST_RESPONSE'
    __str__ = str.__str__

class AssignWidgetKind(str, Enum):
    """The kind of assign widget."""
    SEARCH = 'SEARCH'
    CHOICE = 'CHOICE'
    SLIDER = 'SLIDER'
    CUSTOM = 'CUSTOM'
    STRING = 'STRING'
    STATE_CHOICE = 'STATE_CHOICE'
    PROXY = 'PROXY'
    __str__ = str.__str__

class CatalogValueKind(str, Enum):
    """The kind of value a catalog component prop accepts or a catalog operation argument/return carries."""
    STRING = 'STRING'
    INT = 'INT'
    FLOAT = 'FLOAT'
    BOOL = 'BOOL'
    DICT = 'DICT'
    LIST = 'LIST'
    ANY = 'ANY'
    CALLBACK = 'CALLBACK'
    __str__ = str.__str__

class DemandKind(str, Enum):
    """No documentation"""
    ARGS = 'ARGS'
    RETURNS = 'RETURNS'
    __str__ = str.__str__

class DescriptorOperator(str, Enum):
    """The operator of a requires/provides descriptor: how a port's constraint compares the object's value at `key` with `value`."""
    MATCHES = 'MATCHES'
    EXISTS = 'EXISTS'
    LTE = 'LTE'
    GTE = 'GTE'
    EQUALS = 'EQUALS'
    CONTAINS = 'CONTAINS'
    NOT_EQUALS = 'NOT_EQUALS'
    IN = 'IN'
    NOT_IN = 'NOT_IN'
    __str__ = str.__str__

class DiagnosticLevel(str, Enum):
    """Severity of a registration finding. Errors are never stored (they abort registration), so only WARNING exists."""
    WARNING = 'WARNING'
    __str__ = str.__str__

class EffectClass(str, Enum):
    """The effect class of an implementation — declared by the implementation, never the caller. NONE work is freely retryable/reclaimable; PHYSICAL work touches the real world (no UPSERT), so an ambiguous failure is terminal and must not be retried."""
    NONE = 'NONE'
    PHYSICAL = 'PHYSICAL'
    __str__ = str.__str__

class EffectKind(str, Enum):
    """The kind of effect."""
    MESSAGE = 'MESSAGE'
    HIDE = 'HIDE'
    CUSTOM = 'CUSTOM'
    __str__ = str.__str__

class HookKind(str, Enum):
    """No documentation"""
    CLEANUP = 'CLEANUP'
    INIT = 'INIT'
    __str__ = str.__str__

class LogLevel(str, Enum):
    """No documentation"""
    DEBUG = 'DEBUG'
    INFO = 'INFO'
    ERROR = 'ERROR'
    WARN = 'WARN'
    CRITICAL = 'CRITICAL'
    __str__ = str.__str__

class OptionKey(str, Enum):
    """No documentation"""
    LABEL = 'LABEL'
    DESCRIPTION = 'DESCRIPTION'
    LOGO = 'LOGO'
    VALUE = 'VALUE'
    __str__ = str.__str__

class Ordering(str, Enum):
    """No documentation"""
    ASC = 'ASC'
    ASC_NULLS_FIRST = 'ASC_NULLS_FIRST'
    ASC_NULLS_LAST = 'ASC_NULLS_LAST'
    DESC = 'DESC'
    DESC_NULLS_FIRST = 'DESC_NULLS_FIRST'
    DESC_NULLS_LAST = 'DESC_NULLS_LAST'
    __str__ = str.__str__

class PortKind(str, Enum):
    """The kind of a port: its structural type. Decides which of children, identifier and choices the port must, may or must not carry (see docs/design/ports.md)."""
    INT = 'INT'
    'An integer. No children; choices optional.'
    STRING = 'STRING'
    'A string. No children; choices optional.'
    STRUCTURE = 'STRUCTURE'
    'A reference to an object held by a service, typed by `identifier` (@package/key, required). Values are ids. No children.'
    LIST = 'LIST'
    "A list; exactly one child describes the item type (conventionally keyed '...')."
    BOOL = 'BOOL'
    'A boolean. No children.'
    DICT = 'DICT'
    "A string-keyed map. One child keyed '...' describes a homogeneous value type; several named children describe the known keys."
    FLOAT = 'FLOAT'
    'A floating point number. No children; choices optional.'
    DATE = 'DATE'
    'An ISO-8601 date or datetime string. No children.'
    UNION = 'UNION'
    'One of several variants; at least two children, each a variant.'
    ENUM = 'ENUM'
    'One of a fixed set of values; `choices` required.'
    MODEL = 'MODEL'
    'An object with named fields; at least one child per field, `identifier` optional.'
    MEMORY_STRUCTURE = 'MEMORY_STRUCTURE'
    "A reference to an object that lives in the agent's memory, typed by `identifier` (required). Makes the action LOCAL-scoped. No children."
    INTERFACE = 'INTERFACE'
    'A reference to any object implementing an interface, typed by `identifier` (required). No children.'
    QUANTITY = 'QUANTITY'
    'A physical quantity with a unit; `reference_unit` required, `dimension` derived. No children.'
    __str__ = str.__str__

class ReturnWidgetKind(str, Enum):
    """The kind of return widget."""
    CHOICE = 'CHOICE'
    CUSTOM = 'CUSTOM'
    __str__ = str.__str__

class TaskEventKind(str, Enum):
    """The event kind of the taskevent"""
    BOUND = 'BOUND'
    QUEUED = 'QUEUED'
    STARTED = 'STARTED'
    PROGRESS = 'PROGRESS'
    DELEGATE = 'DELEGATE'
    UNASSIGN = 'UNASSIGN'
    DISCONNECTED = 'DISCONNECTED'
    YIELD = 'YIELD'
    COMPLETED = 'COMPLETED'
    LOG = 'LOG'
    CANCELLING = 'CANCELLING'
    CANCELLED = 'CANCELLED'
    INTERRUPTING = 'INTERRUPTING'
    INTERRUPTED = 'INTERRUPTED'
    PAUSING = 'PAUSING'
    PAUSED = 'PAUSED'
    RESUMING = 'RESUMING'
    RESUMED = 'RESUMED'
    FAILED = 'FAILED'
    CRITICAL = 'CRITICAL'
    __str__ = str.__str__

class TaskInstructKind(str, Enum):
    """The event kind of the taskevent"""
    ASSIGN = 'ASSIGN'
    CANCEL = 'CANCEL'
    RESUME = 'RESUME'
    PAUSE = 'PAUSE'
    INTERRUPT = 'INTERRUPT'
    COLLECT = 'COLLECT'
    __str__ = str.__str__

class WindowFunction(str, Enum):
    """Aggregation computed over a tracked value within a window."""
    MEAN = 'MEAN'
    MIN = 'MIN'
    MAX = 'MAX'
    SUM = 'SUM'
    COUNT = 'COUNT'
    LAST = 'LAST'
    FIRST = 'FIRST'
    STD = 'STD'
    __str__ = str.__str__

class ChoiceAssignWidgetInput(BaseModel):
    """A dropdown over the port's `choices`."""
    kind: Literal['CHOICE'] = Field(default='CHOICE')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    placeholder: str | None = Field(default=None, description="The placeholder text shown before a choice is made. The choices themselves are the port's `choices`.")

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ChoiceReturnWidgetInput(BaseModel):
    """Displays the port's `choices` label for a returned value."""
    kind: Literal['CHOICE'] = Field(default='CHOICE')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CustomAssignWidgetInput(BaseModel):
    """A catalog component rendered as the port's widget."""
    kind: Literal['CUSTOM'] = Field(default='CUSTOM')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    component: str = Field(description='The catalog component to render. The port value is in scope as the reserved root `value`.')
    props: tuple['ComponentPropInput', ...] | None = Field(default=None, description='Props of the component. value_paths may only reference `value` and `dependencies`; agent calls are not allowed.')
    dependencies: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The other ports (port paths, `..` traverses children) whose values the props may reference.')
    'The other ports (port paths, `..` traverses children) whose values the props may reference.\nDefault: []'
    fallback: 'AssignWidgetInput | None' = Field(default=None, description='Widget to render when the UI has no such component in its catalog.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CustomReturnWidgetInput(BaseModel):
    """A catalog component rendered for a returned value."""
    kind: Literal['CUSTOM'] = Field(default='CUSTOM')
    component: str = Field(description='The catalog component to render. The returned value is in scope as the reserved root `value`.')
    props: tuple['ComponentPropInput', ...] | None = Field(default=None, description='Props of the component; value_paths may only reference `value`, agent calls are not allowed.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ProxyAssignWidgetInput(BaseModel):
    """Delegates the port to a port of another action."""
    kind: Literal['PROXY'] = Field(default='PROXY')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    target_port: str = Field(validation_alias=AliasChoices('target_port', 'targetPort'), serialization_alias='targetPort', description='The port key on the targeted action.')
    target_action: str = Field(validation_alias=AliasChoices('target_action', 'targetAction'), serialization_alias='targetAction', description='The action to target: an action-dependency key of `target_dependency` when that is set.')
    target_dependency: str | None = Field(validation_alias=AliasChoices('target_dependency', 'targetDependency'), serialization_alias='targetDependency', default=None, description='The agent dependency (by key) that provides the targeted action; omitted: the implementing agent itself.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class SearchAssignWidgetInput(SearchWidgetInputTrait, BaseModel):
    """A search over a ward for STRUCTURE ports (or lists of them)."""
    kind: Literal['SEARCH'] = Field(default='SEARCH')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    query: SearchQuery = Field(description='The GraphQL query the ward executes to populate the choices. Must be a single `query` operation declaring `$search: String` and `$values: [ID!]`, plus one variable per filter port key.')
    ward: str = Field(description='The ward (service) that executes the query.')
    filters: tuple['ArgPortInput', ...] | None = Field(default=None, description='Filter ports whose values are passed to the query as variables named by their keys.')
    dependencies: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The other ports (port paths, `..` traverses children) whose values the query may reference.')
    'The other ports (port paths, `..` traverses children) whose values the query may reference.\nDefault: []'
    placeholder: str | None = Field(default=None, description='The placeholder text.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class SliderAssignWidgetInput(SliderWidgetInputTrait, BaseModel):
    """A numeric slider for INT, FLOAT and QUANTITY ports."""
    kind: Literal['SLIDER'] = Field(default='SLIDER')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    min: float | None = Field(default=None, description='The minimum value.')
    max: float | None = Field(default=None, description='The maximum value.')
    step: float | None = Field(default=None, description='The step between selectable values; must be positive.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StateChoiceAssignWidgetInput(StateChoiceWidgetInputTrait, BaseModel):
    """A choice over entries of an agent's state."""
    kind: Literal['STATE_CHOICE'] = Field(default='STATE_CHOICE')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    dependency: str | None = Field(default=None, description="The agent dependency (by key) whose state provides the choices; omitted: the implementing agent's own state.")
    state_path: str | None = Field(validation_alias=AliasChoices('state_path', 'statePath'), serialization_alias='statePath', default=None, description='Static JSON pointer into the state value that provides the choices. Mutually exclusive with `state_call`.')
    state_call: 'UtilCallInput | None' = Field(validation_alias=AliasChoices('state_call', 'stateCall'), serialization_alias='stateCall', default=None, description='Pure UtilCall returning that pointer dynamically; may reference `state`, `value` and `dependencies`. Mutually exclusive with `state_path`.')
    state_accessors: tuple['StateAccessorInput', ...] | None = Field(validation_alias=AliasChoices('state_accessors', 'stateAccessors'), serialization_alias='stateAccessors', default=None, description='How to read label/description/logo/value out of each state entry; each accessor is a static pointer or a pure call.')
    dependencies: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The other ports (port paths, `..` traverses children) whose values the calls may reference.')
    'The other ports (port paths, `..` traverses children) whose values the calls may reference.\nDefault: []'

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StringAssignWidgetInput(BaseModel):
    """A text input for STRING ports."""
    kind: Literal['STRING'] = Field(default='STRING')
    follow_value: str | None = Field(validation_alias=AliasChoices('follow_value', 'followValue'), serialization_alias='followValue', default=None, description='Port path of another port whose value this widget follows and mirrors.')
    placeholder: str | None = Field(default=None, description='The placeholder text.')
    as_paragraph: bool | None = Field(validation_alias=AliasChoices('as_paragraph', 'asParagraph'), serialization_alias='asParagraph', default=None, description='Render as a multi-line paragraph.')

    def model_post_init(self, context):
        self.__pydantic_fields_set__.update({'kind'})
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionArgumentInput(BaseModel):
    """A JSON-serializable argument entry for a multi-agent action trigger."""
    key: str | None = Field(default=None, description='The argument property name.')
    value_literal: JSONSerializable | None = Field(validation_alias=AliasChoices('value_literal', 'valueLiteral'), serialization_alias='valueLiteral', default=None, description='Static literal value if not dynamically bound.')
    value_path: str | None = Field(validation_alias=AliasChoices('value_path', 'valuePath'), serialization_alias='valuePath', default=None, description='JSON Pointer referencing the shared Blok state to inject into this argument slot dynamically.')
    agent_call: 'AgentProbeInput | None' = Field(validation_alias=AliasChoices('agent_call', 'agentCall'), serialization_alias='agentCall', default=None, description='Defines a nested agent call if this argument should trigger an agent interaction.')
    util_call: 'UtilCallInput | None' = Field(validation_alias=AliasChoices('util_call', 'utilCall'), serialization_alias='utilCall', default=None, description='Defines a nested utility call if this argument should trigger a system utility interaction.')
    value_list: tuple['ActionArgumentInput', ...] | None = Field(validation_alias=AliasChoices('value_list', 'valueList'), serialization_alias='valueList', default=None, description='Defines a list of values if this argument should be an array.')
    value_dict: tuple['ActionArgumentInput', ...] | None = Field(validation_alias=AliasChoices('value_dict', 'valueDict'), serialization_alias='valueDict', default=None, description='Defines a list of key-value pairs if this argument should be a dictionary.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionDemandInput(BaseModel):
    """Pure matching criteria for an action: hash or name short-circuits, arg/return
    port matches, protocols and forced port counts. Used directly by query filters and, wrapped
    in an ActionDependencyInput, by dependency declarations."""
    hash: str | None = Field(default=None, description='The exact hash of the action. When set, matching short-circuits on the hash and everything else is ignored.')
    key: str | None = Field(default=None, description="The action's key within its app, e.g. 'open_image'. Together with `app` this is the preferred identification of the demanded action.")
    app: str | None = Field(default=None, description="The identifier of the app providing the action, e.g. 'imagej'. Omit (or drop when loosening) to allow equivalent actions from any app.")
    version: str | None = Field(default=None, description='The exact version of the action.')
    name: str | None = Field(default=None, description='The display name of the action to match.')
    arg_matches: tuple['PortMatchInput', ...] | None = Field(validation_alias=AliasChoices('arg_matches', 'argMatches'), serialization_alias='argMatches', default=None, description="The matches the action's arg ports must satisfy.")
    return_matches: tuple['PortMatchInput', ...] | None = Field(validation_alias=AliasChoices('return_matches', 'returnMatches'), serialization_alias='returnMatches', default=None, description="The matches the action's return ports must satisfy.")
    protocols: tuple[str, ...] | None = Field(default=None, description="Protocols (by name) the action must implement, e.g. 'predicate'.")
    force_arg_length: int | None = Field(validation_alias=AliasChoices('force_arg_length', 'forceArgLength'), serialization_alias='forceArgLength', default=None, description='Require that the action has exactly this number of root args.')
    force_return_length: int | None = Field(validation_alias=AliasChoices('force_return_length', 'forceReturnLength'), serialization_alias='forceReturnLength', default=None, description='Require that the action has exactly this number of root returns.')
    pure: bool | None = Field(default=None, description='Require the action to be (or not be) pure. Omit to match either.')
    idempotent: bool | None = Field(default=None, description='Require the action to be (or not be) idempotent. Omit to match either.')
    stateful: bool | None = Field(default=None, description='Require the action to be (or not be) stateful. Omit to match either.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionDependencyInput(BaseModel):
    """A named action requirement of a dependency: a slot key plus the demand the
    resolved action must satisfy, and resolution-lifecycle filters."""
    key: str = Field(description='The local slot key of this action requirement — callers reference it when assigning.')
    description: str | None = Field(default=None, description='The description of the dependency, why it is needed and what it is used for.')
    demand: ActionDemandInput | None = Field(default=None, description='The matching criteria the resolved action must satisfy (app/key preferred; matches loosen).')
    optional: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description="Whether the dependency is optional or not. If the dependency is optional, the agent doesn't have to provide it to be potentially callable")
    "Whether the dependency is optional or not. If the dependency is optional, the agent doesn't have to provide it to be potentially callable\nDefault: False"
    allow_inactive: Annotated[bool | None, GraphQLDefault('True')] = Field(validation_alias=AliasChoices('allow_inactive', 'allowInactive'), serialization_alias='allowInactive', default=None, description='Allow inactive nodes, defaults to true')
    'Allow inactive nodes, defaults to true\nDefault: True'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionFilter(BaseModel):
    """No documentation"""
    name: 'StrFilterLookup | None' = None
    and_: 'ActionFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ActionFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ActionFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    search: str | None = Field(default=None, description="Search by name: a case-insensitive substring, or semantic similarity of the query to the action's name and description. Substring matches rank first, then by similarity; an explicit `ordering` replaces that ranking.")
    ids: tuple[ID, ...] | None = None
    demands: tuple['PortDemandInput', ...] | None = None
    object_demands: tuple['PortDemandInput', ...] | None = Field(validation_alias=AliasChoices('object_demands', 'objectDemands'), serialization_alias='objectDemands', default=None)
    protocols: tuple[str, ...] | None = None
    kind: ActionKind | None = None
    in_collection: str | None = Field(validation_alias=AliasChoices('in_collection', 'inCollection'), serialization_alias='inCollection', default=None)
    used_before: datetime | None = Field(validation_alias=AliasChoices('used_before', 'usedBefore'), serialization_alias='usedBefore', default=None)
    used_after: datetime | None = Field(validation_alias=AliasChoices('used_after', 'usedAfter'), serialization_alias='usedAfter', default=None)
    stateful: bool | None = None
    app_identifier: str | None = Field(validation_alias=AliasChoices('app_identifier', 'appIdentifier'), serialization_alias='appIdentifier', default=None, description='Filter using app identifier')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionOrderDefinedAt(BaseModel):
    """'definedAt' variant of the @oneOf input 'ActionOrder'"""
    defined_at: Ordering = Field(validation_alias=AliasChoices('defined_at', 'definedAt'), serialization_alias='definedAt')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ActionOrderUsedAt(BaseModel):
    """'usedAt' variant of the @oneOf input 'ActionOrder'"""
    used_at: Ordering = Field(validation_alias=AliasChoices('used_at', 'usedAt'), serialization_alias='usedAt')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
ActionOrder = ActionOrderDefinedAt | ActionOrderUsedAt

class AgentDependencyInput(BaseModel):
    """A dependency for a implementation. By defining dependencies, you can
    create a dependency graph for your implementations and actions"""
    key: str = Field(description='The key of this dependency, when assigning you can reference this key to specify which agent_dependency you are assigning to.')
    app: str | None = Field(default=None, description='Which app this dependency corresponds to (i.e. do you want to use a stardist agent for that or imagej agents needs to be a world unique classsifier (reverse domain notation) that identifies the type of agent you want to use, and then we can have multiple agents of the same type running in the system, e.g. startdist could be the app for all agents that correpsond to a startdist instance)')
    version: str | None = Field(default=None, description='The version of the app this dependency corresponds to.')
    name: str | None = Field(default=None, description='The name of the agent. This is used to identify the agent in the system.')
    description: str | None = Field(default=None, description='A description of the dependency, why it is needed and what it is used for. This can be used to provide more context to users when assigning dependencies.')
    optional: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the dependency is optional or not. If the dependency is optional, users can choose to not provide it')
    'Whether the dependency is optional or not. If the dependency is optional, users can choose to not provide it\nDefault: False'
    action_dependencies: tuple[ActionDependencyInput, ...] | None = Field(validation_alias=AliasChoices('action_dependencies', 'actionDependencies'), serialization_alias='actionDependencies', default=None, description='The named action requirements of the agent — each a slot key plus the demand the resolved action must satisfy.')
    state_dependencies: tuple['StateDependencyInput', ...] | None = Field(validation_alias=AliasChoices('state_dependencies', 'stateDependencies'), serialization_alias='stateDependencies', default=None, description="The named state requirements of the agent — each a slot key plus the demand the agent's state must satisfy.")
    auto_resolvable: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('auto_resolvable', 'autoResolvable'), serialization_alias='autoResolvable', default=None, description='Whether this dependency is auto resolvable or not. If so we will try to automatically resolve it based on the demands specified in the dependency and the capabilities of the available agents in the system. This is used to identify the demand in the system. Attention if any of the dependencies of this agent dependency is not auto resolvable, this dependency will also not be auto resolvable')
    'Whether this dependency is auto resolvable or not. If so we will try to automatically resolve it based on the demands specified in the dependency and the capabilities of the available agents in the system. This is used to identify the demand in the system. Attention if any of the dependencies of this agent dependency is not auto resolvable, this dependency will also not be auto resolvable\nDefault: False'
    mutually_exclusive_keys: tuple[str, ...] | None = Field(validation_alias=AliasChoices('mutually_exclusive_keys', 'mutuallyExclusiveKeys'), serialization_alias='mutuallyExclusiveKeys', default=None, description='A list of keys of other agent dependencies that are mutually exclusive with this one. This means two agent dependencies with mutually exclusive keys cannot be assigned to the same implementing agent. This is used to identify the demand in the system.')
    min_viable_instances: int | None = Field(validation_alias=AliasChoices('min_viable_instances', 'minViableInstances'), serialization_alias='minViableInstances', default=None, description='The minimum amount of viable instances for the agent. This is used to identify the demand in the system.')
    max_viable_instances: int | None = Field(validation_alias=AliasChoices('max_viable_instances', 'maxViableInstances'), serialization_alias='maxViableInstances', default=None, description='The maximum amount of viable instances for the agent. This is used to identify the demand in the system.')
    prefered_instances: int | None = Field(validation_alias=AliasChoices('prefered_instances', 'preferedInstances'), serialization_alias='preferedInstances', default=None, description='The prefered amount of instances for the agent. This is used to identify the demand in the system.')
    assign_policy: Annotated[AssignPolicy | None, GraphQLDefault('BALANCED')] = Field(validation_alias=AliasChoices('assign_policy', 'assignPolicy'), serialization_alias='assignPolicy', default=None, description='The policy used to pick which instance of the agent to assign to.')
    'The policy used to pick which instance of the agent to assign to.\nDefault: BALANCED'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class AgentFilter(BaseModel):
    """A way to filter agents"""
    and_: 'AgentFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'AgentFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'AgentFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    client_id: str | None = Field(validation_alias=AliasChoices('client_id', 'clientId'), serialization_alias='clientId', default=None, description='Filter by client ID of the app the agent is registered to')
    ids: tuple[ID, ...] | None = Field(default=None, description='Filter by IDs of the agents')
    has_implementations: tuple[str, ...] | None = Field(validation_alias=AliasChoices('has_implementations', 'hasImplementations'), serialization_alias='hasImplementations', default=None, description='Filter by implementations of the agents')
    has_states: tuple[str, ...] | None = Field(validation_alias=AliasChoices('has_states', 'hasStates'), serialization_alias='hasStates', default=None, description='Filter by states of the agents')
    pinned: bool | None = Field(default=None, description='Filter by pinned agents')
    search: str | None = Field(default=None, description='Filter by name of the agents')
    dependency: ID | None = None
    blok_dependency: ID | None = Field(validation_alias=AliasChoices('blok_dependency', 'blokDependency'), serialization_alias='blokDependency', default=None)
    three_d_model: ID | None = Field(validation_alias=AliasChoices('three_d_model', 'threeDModel'), serialization_alias='threeDModel', default=None)
    distinct: bool | None = None
    action_demands: tuple[ActionDemandInput, ...] | None = Field(validation_alias=AliasChoices('action_demands', 'actionDemands'), serialization_alias='actionDemands', default=None)
    state_demands: tuple['StateDemandInput', ...] | None = Field(validation_alias=AliasChoices('state_demands', 'stateDemands'), serialization_alias='stateDemands', default=None)
    user: ID | None = Field(default=None, description='Filter by user ID')
    app_identifier: ID | None = Field(validation_alias=AliasChoices('app_identifier', 'appIdentifier'), serialization_alias='appIdentifier', default=None, description='Filter using app identifier')
    version_number: str | None = Field(validation_alias=AliasChoices('version_number', 'versionNumber'), serialization_alias='versionNumber', default=None, description='Filter based on version string')
    device_id: ID | None = Field(validation_alias=AliasChoices('device_id', 'deviceId'), serialization_alias='deviceId', default=None, description='Filter based on device')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class AgentInput(BaseModel):
    """No documentation"""
    name: str | None = Field(default=None, description='The name of the agent. This is used to identify the agent in the system.')
    description: str | None = Field(default=None, description='What this agent is, in a sentence. Omitting it leaves whatever the agent already has: a name is what identifies it, a description is what tells two of them apart.')
    kind: AgentKind | None = Field(default=None, description='The transport kind of the agent: WEBSOCKET (default) or WEBHOOK (a HookAgent the backend POSTs to).')
    hook_url: str | None = Field(validation_alias=AliasChoices('hook_url', 'hookUrl'), serialization_alias='hookUrl', default=None, description='For a WEBHOOK agent: the URL the backend POSTs messages (Assign, Cancel, Caller* events) to.')
    hook_url_secret: str | None = Field(validation_alias=AliasChoices('hook_url_secret', 'hookUrlSecret'), serialization_alias='hookUrlSecret', default=None, description='For a WEBHOOK agent: the shared secret used to HMAC-sign messages in both directions (outbound delivery and POST intake).')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class AgentProbeInput(BaseModel):
    """Defines a callback that routes user interactions directly to an Arkitekt Agent via Rekuest."""
    dependency: str = Field(description="The abstract agent dependency key declared in the Blok manifest (e.g., 'stage_dep').")
    operation: str = Field(description="The target function name registered on that specific agent's worker thread loop.")
    arguments: tuple[ActionArgumentInput, ...] | None = Field(default=None, description='Key-value arguments map compiled for the target agent call.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ArgPortInput(PortTrait, BaseModel):
    """A Port is a single input or output of an action, identified by its `key` and typed by its `kind`.

    STRUCTURE, MEMORY_STRUCTURE and INTERFACE ports carry an `identifier` of the form `@package/key`
    (e.g. `@mikro/image`); ports with the same identifier are compatible. LIST and DICT ports have one
    child (the item type), UNION ports two or more (the variants), MODEL ports one per field. ENUM ports
    declare `choices`. See docs/design/ports.md for the full table.
    """
    key: str = Field(description="The key of the port: unique among its siblings, free of '..', not 'value'. LIST/DICT item ports are conventionally keyed '...'.")
    label: str | None = Field(default=None, description='The label of the port. This is the text that is displayed in the UI')
    kind: PortKind = Field(description='The kind of the port. This is the type of the port. Can be either int, string, structure, list, bool, dict, float, date, union or model')
    description: str | None = Field(default=None, description='The description of the port. This is the text that is displayed in the UI when the user hovers over the port')
    identifier: str | None = Field(default=None, description="The identifier of the port's type, of the form @package/key. Required for STRUCTURE, MEMORY_STRUCTURE and INTERFACE, where it is the only identity a value has; optional for MODEL and ENUM, where it names the class or enum the port was built from so that agents can map a value back to it.")
    nullable: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the port is nullable or not. If the port is nullable, it can be set to null. If the port is not nullable, it cannot be set to null')
    'Whether the port is nullable or not. If the port is nullable, it can be set to null. If the port is not nullable, it cannot be set to null\nDefault: False'
    effects: tuple['EffectInput', ...] | None = Field(default=None, description='The effects of the port')
    choices: tuple['ChoiceInput', ...] | None = Field(default=None, description='The values the port accepts (required for ENUM; optional for INT, FLOAT, STRING). Rendered by CHOICE widgets.')
    reference_unit: str | None = Field(validation_alias=AliasChoices('reference_unit', 'referenceUnit'), serialization_alias='referenceUnit', default=None, description='For QUANTITY ports: the canonical/reference unit of the physical quantity, e.g. "volt" or "farad". It is the default selection and the key used to resolve the concrete quantity type; other units of the same dimension are still allowed.')
    proposed_units: tuple[str, ...] | None = Field(validation_alias=AliasChoices('proposed_units', 'proposedUnits'), serialization_alias='proposedUnits', default=None, description='For QUANTITY ports: units offered as a dropdown in the UI, e.g. ["pF", "nF", "uF"]. Proposals only — any unit of the same dimension remains valid input.')
    dimension: str | None = Field(default=None, description='For QUANTITY ports: the pint dimensionality string, e.g. "[mass] * [length] ** 2 / [time] ** 3 / [current]". This is the wiring-compatibility key between quantity ports.')
    children: tuple['ArgPortInput', ...] | None = Field(default=None, description='The child ports (used for list, dict, union and model ports).')
    validators: tuple['ValidatorInput', ...] | None = Field(default=None, description='The validators for the port')
    default: Any | None = Field(default=None, description="The default value for the port; must fit the port's kind.")
    widget: 'AssignWidgetInput | None' = Field(default=None, description='The assign widget to use for this port, discriminated by `kind`.')
    requires: tuple['RequiresInput', ...] | None = Field(default=None, description="The descriptors for the port. Descriptors are key-value pairs that can be used to add additional metadata to a port. When using rekuest's action search, you can filter actions based on their port descriptors")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class AssignInput(BaseModel):
    """The input for assigning args to a action. A GraphQL assign is a ROOT by definition — children are created only over the agent socket (AssignRequest, where parent is mandatory) and by server-internal paths like init hooks, so parent/dependency/method are deliberately absent here."""
    action: ID | None = Field(default=None, description='The action ID to assign to')
    resolution: ID | None = Field(default=None, description='The resolution ID to assign to when assining to a implementation with dependencies')
    implementation: ID | None = Field(default=None, description='The implementation ID to assign to when directly assingint to a implementation')
    agent: ID | None = Field(default=None, description='The agent ID to assign to when directly assingint to a implementation')
    action_hash: ActionHash | None = Field(validation_alias=AliasChoices('action_hash', 'actionHash'), serialization_alias='actionHash', default=None, description='The hash of the action. This is used to identify the action in the system.')
    interface: str | None = Field(default=None, description='The interface of the implementation. Only ussable if you also set agent')
    hooks: tuple['HookInput', ...] | None = Field(default=None, description='The hooks of the task. This is used to identify the task in the system.')
    args: Args = Field(description='The args of the task. Its a dictionary of ports and values')
    reference: str | None = Field(default=None, description='The reference of the task. This is used to identify the task in the system.')
    capture: bool = Field(description='Whether to capture the task.')
    dependencies: tuple['ResolvedDependencyInput', ...] | None = Field(default=None, description='The dependencies of the task. This maps dependency keys to implementation IDs.')
    step: bool | None = Field(default=None, description='Whether the task should step. Ie. go to the next breakpoint')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
AssignWidgetInput = Annotated[ChoiceAssignWidgetInput | CustomAssignWidgetInput | ProxyAssignWidgetInput | SearchAssignWidgetInput | SliderAssignWidgetInput | StateChoiceAssignWidgetInput | StringAssignWidgetInput, Field(discriminator='kind')]

class BlokAgentMappingInput(BaseModel):
    """The input for updating a blok."""
    agent: ID
    key: str
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class BlokImplementationInput(BlokImplementationInputTrait, BaseModel):
    """Which locks does the agent provide in general"""
    key: str = Field(description='The key of this Blok implementation.')
    dependencies: Annotated[tuple[AgentDependencyInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The dependencies required by this Blok.')
    'The dependencies required by this Blok.\nDefault: []'
    components: tuple['ComponentNodeInput', ...] = Field(description='The UI component tree blueprint for this Blok.')
    catalog: str | None = Field(default=None, description="The optional catalog name if this Blok should be registered inside a specific namespace in your Electron app's UI component registry.")
    description: str | None = Field(default=None, description="A human-readable description about this Blok's purpose and functionality.")
    demo_state: JSONSerializable | None = Field(validation_alias=AliasChoices('demo_state', 'demoState'), serialization_alias='demoState', default=None, description="An optional JSON-serializable object providing demo state values for this Blok's internal reactive data model, useful for testing and development purposes.")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CancelInput(BaseModel):
    """The input for canceling a task."""
    task: ID = Field(description='The task ID to cancel')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CatalogArgumentInput(BaseModel):
    """An argument a catalog operation accepts."""
    key: str = Field(description='The argument key a UtilCall argument must use.')
    kind: CatalogValueKind = Field(description='The value kind of the argument.')
    required: Annotated[bool | None, GraphQLDefault('True')] = Field(default=None, description='Whether every call must pass this argument.')
    'Whether every call must pass this argument.\nDefault: True'
    description: str | None = Field(default=None, description='Human-readable description of the argument.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CatalogComponentInput(BaseModel):
    """A component a UI catalog can render."""
    name: str = Field(description="The component name a ComponentNode.component (or a custom widget's component) must match.")
    description: str | None = Field(default=None, description='Human-readable description of the component.')
    props: Annotated[tuple['CatalogPropInput', ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The props this component accepts.')
    'The props this component accepts.\nDefault: []'
    accepts_children: Annotated[bool | None, GraphQLDefault('True')] = Field(validation_alias=AliasChoices('accepts_children', 'acceptsChildren'), serialization_alias='acceptsChildren', default=None, description='Whether ComponentNode.children may be nested under this component.')
    'Whether ComponentNode.children may be nested under this component.\nDefault: True'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CatalogOperationInput(BaseModel):
    """A pure operation a UI catalog can evaluate for UtilCalls."""
    name: str = Field(description='The operation name a UtilCall.operation must match.')
    description: str | None = Field(default=None, description='Human-readable description of the operation.')
    arguments: Annotated[tuple[CatalogArgumentInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The arguments the operation accepts.')
    'The arguments the operation accepts.\nDefault: []'
    returns: CatalogValueKind = Field(description='The kind of value the operation returns (BOOL for effect and validator calls).')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CatalogPropInput(BaseModel):
    """A prop a catalog component accepts."""
    key: str = Field(description='The prop key a ComponentProp.key must match.')
    kind: CatalogValueKind = Field(description='The value kind this prop accepts. CALLBACK props must be bound via agent_call or util_call.')
    required: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether every component instance must set this prop.')
    'Whether every component instance must set this prop.\nDefault: False'
    description: str | None = Field(default=None, description='Human-readable description of the prop.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ChoiceInput(BaseModel):
    """
A choice is a value that can be selected in a dropdown.

It is composed of a value, a label, and a description. The value is the
value that is returned when the choice is selected. The label is the
text that is displayed in the dropdown. The description is the text
that is displayed when the user hovers over the choice.

    """
    value: Any = Field(description="The value of the choice (any JSON value); must fit the port's kind. This is the value that is returned when the choice is selected")
    label: str = Field(description='The label of the choice. This is the text that is displayed in the UI')
    image: str | None = Field(default=None, description='The image of the choice. This is the image that is displayed in the UI (must be a URL)')
    description: str | None = Field(default=None, description='The description of the choice. This is the text that is displayed in the UI when the user hovers over the choice')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CollectInput(BaseModel):
    """The input for collecting a shelved item in a drawer."""
    drawers: tuple[ID, ...] = Field(description='The drawer ID to collect')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ComponentNodeInput(BaseModel):
    """An abstract structural visual element inside a Blok blueprint manifest."""
    id: str = Field(description='Unique structural string identifying this node instance inside the flat workspace layout tree.')
    component: str = Field(description="The type indicator token matching your Electron app's registered catalog specs (e.g. 'Slider').")
    props: tuple['ComponentPropInput', ...] | None = Field(default=None, description='The collection of static values, state pointers, or action endpoints assigned to this component.')
    children: tuple['ComponentNodeInput', ...] | None = Field(default=None, description='Flat adjacency pointer list mapping out IDs nested inside this specific component layer.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ComponentPropInput(BaseModel):
    """A single key-value prop configuration for a component layout node."""
    key: str = Field(description='The prop key name matching the target UI catalog constraint.')
    static_value: JSONSerializable | None = Field(validation_alias=AliasChoices('static_value', 'staticValue'), serialization_alias='staticValue', default=None, description="A raw scalar or JSON-stringified literal configuration parameter (e.g. '40x' or True).")
    dynamic_value: 'DynamicValueInput | None' = Field(validation_alias=AliasChoices('dynamic_value', 'dynamicValue'), serialization_alias='dynamicValue', default=None, description='A reactive state data-binding rule.')
    declares_value: str | None = Field(validation_alias=AliasChoices('declares_value', 'declaresValue'), serialization_alias='declaresValue', default=None, description="If set, this prop declares a new 'value' in the Blok state that can be referenced by other props or actions. The value of this field should be the name of the declared value (e.g., 'selected_user').")
    agent_call: AgentProbeInput | None = Field(validation_alias=AliasChoices('agent_call', 'agentCall'), serialization_alias='agentCall', default=None, description='Defines an imperative interactive network action callback loop if this prop should trigger an agent interaction.')
    util_call: 'UtilCallInput | None' = Field(validation_alias=AliasChoices('util_call', 'utilCall'), serialization_alias='utilCall', default=None, description='Defines an imperative interactive network action callback loop if this prop should trigger a system utility interaction.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateBlokInput(CreateBlokInputTrait, BaseModel):
    """The input for creating a blok."""
    name: str = Field(description='The name of the Blok, used for identification in the system.')
    dependencies: tuple[AgentDependencyInput, ...] | None = Field(default=None, description='The dependencies of the blok. This is used to identify the blok in the system.')
    description: str | None = Field(default=None, description='The description of the blok. This can described the blok and its purpose.')
    catalog: str | None = Field(default=None, description='The universal id')
    components: tuple[ComponentNodeInput, ...] | None = Field(default=None, description='The schema of the blok. This can be used to validate the blok input and output.')
    demo_state: Args | None = Field(validation_alias=AliasChoices('demo_state', 'demoState'), serialization_alias='demoState', default=None, description='The initial state of the blok. This is used to set the initial state of the blok when it is materialized.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateDashboardInput(BaseModel):
    """Input for creating a dashboard, optionally specifying its name and the bloks to include. It is owned by the caller's organization."""
    name: str = Field(description='The name of the dashboard.')
    bloks: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The list of blok IDs to include in the dashboard.')
    'The list of blok IDs to include in the dashboard.\nDefault: []'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateImplementationInput(BaseModel):
    """The input for creating a implementation."""
    implementation: 'ImplementationInput' = Field(description='The implementation to create. This is used to identify the implementation in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateShortcutInput(BaseModel):
    """The input for creating a shortcut."""
    name: str = Field(description='The name of the shortcut. This is used to identify the shortcut in the system.')
    description: str | None = Field(default=None, description='The description of the shortcut.This can described the shortcut and its purpose.')
    action: ID = Field(description='The action ID to create a shortcut for')
    args: Args = Field(description='The arguments to pre-pass to the shortcut. This is used to identify the shortcut in the system.')
    allow_quick: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('allow_quick', 'allowQuick'), serialization_alias='allowQuick', default=None, description='Whether to allow quick shortcuts. Quick shorts are shortcuts that can be autorun without scpeific assignment')
    'Whether to allow quick shortcuts. Quick shorts are shortcuts that can be autorun without scpeific assignment\nDefault: False'
    use_returns: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('use_returns', 'useReturns'), serialization_alias='useReturns', default=None, description='Whether when running the short the returns should be used further. Allows to create mini pipelines')
    'Whether when running the short the returns should be used further. Allows to create mini pipelines\nDefault: False'
    bind_number: int | None = Field(validation_alias=AliasChoices('bind_number', 'bindNumber'), serialization_alias='bindNumber', default=None, description='The bind number of the shortcut. This is used to identify the shortcut in the system.')
    toolbox: ID | None = Field(default=None, description='The toolbox ID to create the shortcut in. If not provided, the shortcut will be created in the default toolbox.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateSpaceInput(BaseModel):
    """The input for creating a space."""
    name: str = Field(description='The name of the space. This is used to identify the space in the system.')
    placements: tuple['PlacementInput', ...] | None = Field(default=None, description='The placements to create in the space. This is used to identify the placements in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateTestCaseInput(BaseModel):
    """No documentation"""
    action: ID
    tester: ID
    description: str | None = None
    name: str | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateTestResultInput(BaseModel):
    """No documentation"""
    case: ID
    tester: ID
    implementation: ID
    passed: bool
    result: str | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class CreateThreeDModelInput(BaseModel):
    """The input for creating a 3D model."""
    name: str = Field(description='The name of the 3D model.')
    description: str | None = Field(default=None, description='A description of the 3D model.')
    media: MediaLike = Field(description='The media store file for the 3D model.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class DefinitionInput(DefinitionInputTrait, BaseModel):
    """A definition

    Definitions are the building implementation for Actions and provide the
    information needed to create a action. They are primarly composed of a name,
    a description, and a list of ports.

    Definitions provide a protocol of input and output, and do not contain
    any information about the actual implementation of the action ( this is handled
    by a implementation that implements a action).

    """
    description: str | None = Field(default=None, description='The description of the definition. This is the text that is displayed in the UI')
    collections: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The collections of the definition. This is used to group definitions together in the UI')
    'The collections of the definition. This is used to group definitions together in the UI\nDefault: []'
    key: str = Field(description='The key of the definition. This is used to uniquely identify the definition')
    version: str = Field(description='The version of the definition. This is used to differentiate if the underyling algorithm has changed, i.e we would expect different results for the same input')
    name: str = Field(description='The name of the actions. This is used to uniquely identify the definition')
    stateful: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the definition is stateful or not. If the definition is stateful, it can be used to create a stateful action. If the definition is not stateful, it cannot be used to create a stateful action')
    'Whether the definition is stateful or not. If the definition is stateful, it can be used to create a stateful action. If the definition is not stateful, it cannot be used to create a stateful action\nDefault: False'
    pure: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the action is pure: same args always produce the same result and no side effects — its results are replayable/cacheable. Implies idempotent. Incompatible with stateful and with a PHYSICAL effect class.')
    'Whether the action is pure: same args always produce the same result and no side effects — its results are replayable/cacheable. Implies idempotent. Incompatible with stateful and with a PHYSICAL effect class.\nDefault: False'
    idempotent: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the action is idempotent: safe to run multiple times with the same args without changing the outcome — on ambiguous executor loss it may be freely re-dispatched.')
    'Whether the action is idempotent: safe to run multiple times with the same args without changing the outcome — on ambiguous executor loss it may be freely re-dispatched.\nDefault: False'
    allow_probe: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('allow_probe', 'allowProbe'), serialization_alias='allowProbe', default=None, description='Whether the action may be invoked as a probe: zero persistence, redis-held state, no history/replay/recovery. Only actions declaring this are callable via the call mutation.')
    'Whether the action may be invoked as a probe: zero persistence, redis-held state, no history/replay/recovery. Only actions declaring this are callable via the call mutation.\nDefault: False'
    catalogs: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description="Names of the UI catalogs that extend the base catalog (`base@1`, always applied) for this definition's effect and validator calls. Unknown names yield an unknown_catalog warning; conflicting operation definitions across catalogs are a registration error.")
    "Names of the UI catalogs that extend the base catalog (`base@1`, always applied) for this definition's effect and validator calls. Unknown names yield an unknown_catalog warning; conflicting operation definitions across catalogs are a registration error.\nDefault: []"
    port_groups: Annotated[tuple['PortGroupInput', ...] | None, GraphQLDefault('[]')] = Field(validation_alias=AliasChoices('port_groups', 'portGroups'), serialization_alias='portGroups', default=None, description='The port groups of the definition. This is used to group ports together in the UI')
    'The port groups of the definition. This is used to group ports together in the UI\nDefault: []'
    args: Annotated[tuple[ArgPortInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The args of the definition. This is the input ports of the definition')
    'The args of the definition. This is the input ports of the definition\nDefault: []'
    returns: Annotated[tuple['ReturnPortInput', ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The returns of the definition. This is the output ports of the definition')
    'The returns of the definition. This is the output ports of the definition\nDefault: []'
    kind: ActionKind = Field(description='The kind of the definition. This is the type of the definition. Can be either a function or a generator')
    is_test_for: Annotated[tuple['TestTargetInput', ...] | None, GraphQLDefault('[]')] = Field(validation_alias=AliasChoices('is_test_for', 'isTestFor'), serialization_alias='isTestFor', default=None, description='The actions this definition is a test for, each identified by hash or by (app, key, version).')
    'The actions this definition is a test for, each identified by hash or by (app, key, version).\nDefault: []'
    is_dev: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('is_dev', 'isDev'), serialization_alias='isDev', default=None, description='Whether the definition is a dev definition or not. If the definition is a dev definition, it can be used to create a dev action. If the definition is not a dev definition, it cannot be used to create a dev action')
    'Whether the definition is a dev definition or not. If the definition is a dev definition, it can be used to create a dev action. If the definition is not a dev definition, it cannot be used to create a dev action\nDefault: False'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class DescriptorInput(BaseModel):
    """A single runtime descriptor key/value pair carried by a candidate object."""
    key: str = Field(description="The descriptor key, e.g. 'axes'.")
    value: Any = Field(description='The descriptor value. Any JSON-serializable value.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class DynamicValueInput(BaseModel):
    """A bound state pointer referencing a variable inside a Blok state instance."""
    literal: str | None = Field(default=None, description='A static fallback literal value (serialized string or JSON primitive) used when `path` does not resolve.')
    path: str | None = Field(default=None, description="JSON Pointer to a variable inside the Blok's isolated data model (e.g., '/microscope/exposure').")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class EffectInput(EffectInputTrait, BaseModel):
    """
    An effect is a way to modify a port based on a condition. For example,
    you could have an effect that hides the port if another port meets a condition,
    e.g. when the user selects a certain option in a dropdown, another port is hidden.

    The condition is a pure blok UtilCall (`call`) evaluated client-side against the
    catalog; it must return a boolean deciding whether the effect applies. `dependencies`
    is the authoritative list of other ports the call may reference (plus `value` for the
    port's own value).
    """
    call: 'UtilCallInput' = Field(description="The pure blok UtilCall, evaluated client-side against the catalog, that decides whether the effect applies. It must return a boolean. Argument value_paths may only reference names listed in `dependencies`, plus `value` for the port's own value.")
    dependencies: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description="The form-field subscription list of the effect: the keys of the other ports whose values the call may reference. This list is authoritative: a value_path in the call may only reference these names (plus `value` for the port's own value). Use the .. syntax to traverse the tree of ports, e.g. 'foo..bar' for the child 'bar' of port 'foo'.")
    "The form-field subscription list of the effect: the keys of the other ports whose values the call may reference. This list is authoritative: a value_path in the call may only reference these names (plus `value` for the port's own value). Use the .. syntax to traverse the tree of ports, e.g. 'foo..bar' for the child 'bar' of port 'foo'.\nDefault: []"
    message: str | None = Field(default=None, description='The message to display when the effect is applied (if it is a message effect)')
    kind: EffectKind = Field(description='The kind of the effect. Can be either message, hide or custom')
    fade: Annotated[bool | None, GraphQLDefault('True')] = Field(default=None, description='Whether to fade out the port when the effect is applied (if it is a hide effect)')
    'Whether to fade out the port when the effect is applied (if it is a hide effect)\nDefault: True'
    source: str | None = Field(default=None, description='The authoring expression the call was compiled from (informational; never parsed or validated by the server).')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class FinishMediaUploadInput(BaseModel):
    """No documentation"""
    store_id: str = Field(validation_alias=AliasChoices('store_id', 'storeId'), serialization_alias='storeId')
    valid: Annotated[bool | None, GraphQLDefault('True')] = None
    'Default: True'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class HookInput(BaseModel):
    """A hook is a function that is called when a action has reached a specific lifecycle point. Hooks are jsut actions that take a task as input and return a value."""
    kind: HookKind = Field(description='The kind of the hook. This is used to identify the hook in the system.')
    hash: ActionHash = Field(description='The hash of the hook. This is used to identify the hook in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ImplementAgentInput(ImplementAgentInputTrait, BaseModel):
    """Implement an agent with the given implementations, states and locks. This will create the agent if it doesn't exist and update it if it does exist."""
    name: str | None = Field(default=None, description='The name of the agent. This is used to identify the agent in the system.')
    description: str | None = Field(default=None, description='What this agent is, in a sentence. Omitting it leaves whatever the agent already has, unlike `name`, which falls back to the client id.')
    states: tuple['StateImplementationInput', ...] | None = Field(default=None, description='The states of the agent. This is used to specify the initial states of the agent')
    implementations: tuple['ImplementationInput', ...] | None = Field(default=None, description='The implementations of the agent. This is used to specify the initial implementations of the agent')
    locks: tuple['LockImplementationInput', ...] | None = Field(default=None, description='The locks of the agent. This is used to specify which resources the agent needs to run')
    bloks: tuple[BlokImplementationInput, ...] | None = Field(default=None, description='The blocks of the agent. This is used to specify the initial blocks of the agent')
    hash: str | None = Field(default=None, description='A unique hash of the agent definition. An agent can use this hash to check if its definition has changed and if it needs to update its implementations and states. This is used to optimize the update process by only updating the implementations and states that have changed.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ImplementationActionFilter(BaseModel):
    """No documentation"""
    and_: 'ImplementationActionFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ImplementationActionFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ImplementationActionFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    search: str | None = None
    name: str | None = None
    ids: tuple[ID, ...] | None = None
    demands: tuple['PortDemandInput', ...] | None = None
    kind: ActionKind | None = None
    protocols: tuple[str, ...] | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ImplementationAgentFilter(BaseModel):
    """No documentation"""
    and_: 'ImplementationAgentFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ImplementationAgentFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ImplementationAgentFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    client_id: str | None = Field(validation_alias=AliasChoices('client_id', 'clientId'), serialization_alias='clientId', default=None)
    ids: tuple[ID, ...] | None = None
    has_implementations: tuple[str, ...] | None = Field(validation_alias=AliasChoices('has_implementations', 'hasImplementations'), serialization_alias='hasImplementations', default=None)
    has_states: tuple[str, ...] | None = Field(validation_alias=AliasChoices('has_states', 'hasStates'), serialization_alias='hasStates', default=None)
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ImplementationFilter(BaseModel):
    """No documentation"""
    interface: 'StrFilterLookup | None' = None
    action: ImplementationActionFilter | None = None
    agent: ImplementationAgentFilter | None = None
    and_: 'ImplementationFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ImplementationFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ImplementationFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    active: bool | None = None
    ids: tuple[ID, ...] | None = None
    action_hash: ActionHash | None = Field(validation_alias=AliasChoices('action_hash', 'actionHash'), serialization_alias='actionHash', default=None)
    parameters: tuple['ParamPair', ...] | None = None
    resolvable_for: ID | None = Field(validation_alias=AliasChoices('resolvable_for', 'resolvableFor'), serialization_alias='resolvableFor', default=None)
    search: str | None = None
    action_demand: ActionDemandInput | None = Field(validation_alias=AliasChoices('action_demand', 'actionDemand'), serialization_alias='actionDemand', default=None)
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ImplementationInput(ImplementationInputTrait, BaseModel):
    """A implementation is a blueprint for a action. It is composed of a definition, a list of dependencies, and a list of params."""
    definition: DefinitionInput = Field(description='The definition of the implementation. This is used to uniquely identify the implementation')
    dependencies: Annotated[tuple[AgentDependencyInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The agent dependencies required by this implementation.')
    'The agent dependencies required by this implementation.\nDefault: []'
    tracks: tuple['TrackInput', ...] | None = Field(default=None, description='The tracks of the definition. This is used to track values over time during the runtime of an action. This is the state of a dependency')
    interface: str | None = Field(default=None, description='The interface of the implementation. This is used to group implementations together in the UI')
    params: Any | None = Field(default=None, description='The params of the implementation. This is used to pass parameters to the implementation')
    instance_id: str | None = Field(validation_alias=AliasChoices('instance_id', 'instanceId'), serialization_alias='instanceId', default=None, description='The instance id of the agent this implementation is bound to.')
    locks: tuple[str, ...] | None = Field(default=None, description='The locks of the implementation. This is used to specify which resources the implementation needs to run')
    optimistics: tuple['OptimisticInput', ...] | None = Field(default=None, description='The optimistics of the definition. This is used to optimistically set state values when the action is assigned, to provide a better user experience.')
    manipulates: tuple[str, ...] | None = Field(default=None, description='The states that the implementation manipulates. This is used to identify which states are manipulated by the implementation, and can be use to enhance state safety in the system')
    needs_token: Annotated[bool | None, GraphQLDefault('True')] = Field(validation_alias=AliasChoices('needs_token', 'needsToken'), serialization_alias='needsToken', default=None, description='Whether Rekuest should mint a signed provenance token when this implementation is assigned. Default true (provenance-by-default); set false for trivial/internal tasks that never produce external provenance.')
    'Whether Rekuest should mint a signed provenance token when this implementation is assigned. Default true (provenance-by-default); set false for trivial/internal tasks that never produce external provenance.\nDefault: True'
    provenance_audience: tuple[str, ...] | None = Field(validation_alias=AliasChoices('provenance_audience', 'provenanceAudience'), serialization_alias='provenanceAudience', default=None, description="The downstream service(s) the provenance token should be scoped to (the token's `aud`). If omitted, Rekuest derives the audience from the structures the assignment acts on.")
    effect: Annotated[EffectClass | None, GraphQLDefault('NONE')] = Field(default=None, description='The effect class of this implementation. NONE work is freely retryable/reclaimable; PHYSICAL work touches the real world and an ambiguous failure is terminal (never retried). Declared by the implementation here — never by the caller.')
    'The effect class of this implementation. NONE work is freely retryable/reclaimable; PHYSICAL work touches the real world and an ambiguous failure is terminal (never retried). Declared by the implementation here — never by the caller.\nDefault: NONE'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class InterruptInput(BaseModel):
    """The input for interrupting a task."""
    task: ID = Field(description='The task ID to interrupt')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class LockDefinitionInput(BaseModel):
    """Which locks does the agent provide in general"""
    key: str = Field(description='The key of the lock. This is used to uniquely identify the lock')
    description: str | None = Field(default=None, description='Describe the lock a bit')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class LockImplementationInput(BaseModel):
    """Which locks does the agent provide in general"""
    key: str = Field(description='The key of the lock implementation.')
    definition: LockDefinitionInput = Field(description='The lock definition this implementation fulfills.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class MappedAgentInput(BaseModel):
    """The input for mapping actions to implementations in a agent."""
    key: str = Field(description='The key of the agent to map. This is used to identify the agent in the system.')
    agent: ID = Field(description='The agent ID to map the actions to. This is used to identify the agent in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class MaterializeBlokInput(BaseModel):
    """The input for creating a blok."""
    blok: ID
    dashboard: ID | None = Field(default=None, description='The dashboard ID to materialize the blok in. If not provided, the blok will be materialized in the default dashboard.')
    agent_mappings: tuple[BlokAgentMappingInput, ...] | None = Field(validation_alias=AliasChoices('agent_mappings', 'agentMappings'), serialization_alias='agentMappings', default=None, description='The agent mappings for the blok. This is used to map the blok dependencies to agents in the system.')
    name: str | None = Field(default=None, description="Display name of this materialization. Defaults to the blok's name.")
    description: str | None = Field(default=None, description="Description of this materialization. Defaults to the blok's description.")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class OffsetPaginationInput(BaseModel):
    """No documentation"""
    offset: Annotated[int | None, GraphQLDefault('0')] = None
    'Default: 0'
    limit: int | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class OptimisticInput(BaseModel):
    """ An optimistic is used to optimistically set state values when the action is assigned. This is used to provide a better user experience by optimistically setting state values when the action is assigned, instead of waiting for the action to be executed and the state to be updated. This will only ever happen on the frontend.

"""
    state: str = Field(description='The state to optimistically set when the action is assigned')
    path: str | None = Field(default=None, description='Static JSON pointer into the state value to set. Mutually exclusive with `path_call`.')
    path_call: 'UtilCallInput | None' = Field(validation_alias=AliasChoices('path_call', 'pathCall'), serialization_alias='pathCall', default=None, description='Pure UtilCall returning the pointer dynamically; may reference `args` (the assignment arguments). Mutually exclusive with `path`.')
    accessor: str | None = Field(default=None, description='Static JSON pointer into the assignment args for the value to set; omitted: the whole args.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ParamPair(BaseModel):
    """No documentation"""
    key: str
    value: str
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class PauseInput(BaseModel):
    """The input for pausing a task."""
    task: ID = Field(description='The task ID to pause')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class PlacementInput(BaseModel):
    """The input for creating or updating a placement."""
    role: str | None = Field(default=None, description='The role of the placement. This is used to identify the placement in the system.')
    affine_matrix: tuple[tuple[float, ...], ...] | None = Field(validation_alias=AliasChoices('affine_matrix', 'affineMatrix'), serialization_alias='affineMatrix', default=None, description='The affine matrix for the placement. This is used to identify the placement in the system.')
    model: ID | None = Field(default=None, description='The 3D model ID for the placement. This is used to identify the 3D model in the system.')
    agent: ID | None = Field(default=None, description='The agent ID for the placement. This is used to identify the agent in the system.')
    blok: ID | None = Field(default=None, description='A specific blok that should be used to visualize the state of the placement.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class PortDemandInput(BaseModel):
    """The input for creating a port demand."""
    kind: DemandKind = Field(description='The kind of the demand. You can ask for args or returns')
    matches: tuple['PortMatchInput', ...] | None = Field(default=None, description='The matches of the demand. ')
    force_length: int | None = Field(validation_alias=AliasChoices('force_length', 'forceLength'), serialization_alias='forceLength', default=None, description='Require that the action has a specific number of ports. This is used to identify the demand in the system.')
    force_non_nullable_length: int | None = Field(validation_alias=AliasChoices('force_non_nullable_length', 'forceNonNullableLength'), serialization_alias='forceNonNullableLength', default=None, description='Require that the action has a specific number of non-nullable ports. This is used to identify the demand in the system.')
    force_structure_length: int | None = Field(validation_alias=AliasChoices('force_structure_length', 'forceStructureLength'), serialization_alias='forceStructureLength', default=None, description='Require that the action has a specific number of structure ports. This is used to identify the demand in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class PortGroupInput(BaseModel):
    """A Port Group is a group of ports that are related to each other. It is used to group ports together in the UI and provide a better user experience."""
    key: str = Field(description='The key of the port group. This is used to uniquely identify the port group')
    title: str | None = Field(default=None, description='The title of the port group, displayed in the UI')
    description: str | None = Field(default=None, description='The description of the port group, displayed in the UI')
    effects: Annotated[tuple[EffectInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The effects applied to the port group as a whole')
    'The effects applied to the port group as a whole\nDefault: []'
    ports: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The keys of the root arg ports in this group; a port belongs to at most one group')
    'The keys of the root arg ports in this group; a port belongs to at most one group\nDefault: []'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class PortMatchInput(BaseModel):
    """A structural (and optionally object-level) match against a port. Purely
    structural fields target the port shape; the optional ``descriptors`` carry a concrete
    runtime object's key/value pairs, evaluated against the port's compiled requires
    micro-constraint to find actions the object can actually be passed to."""
    at: int | None = Field(default=None, description='The index of the port to match.')
    key: str | None = Field(default=None, description='The key of the port to match.')
    kind: PortKind | None = Field(default=None, description='The kind of the port to match.')
    identifier: str | None = Field(default=None, description='The identifier of the port to match.')
    nullable: bool | None = Field(default=None, description='Whether the port is nullable.')
    dimension: str | None = Field(default=None, description='The canonical pint dimensionality the port must have (QUANTITY wiring-compatibility key).')
    descriptors: tuple[DescriptorInput, ...] | None = Field(default=None, description="Runtime descriptors of a candidate object, evaluated against the port's compiled requires micro-constraint. Omit for purely structural matching.")
    children: tuple['PortMatchInput', ...] | None = Field(default=None, description='The matches for the children of the port to match.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ProvidesInput(BaseModel):
    """No documentation"""
    key: str = Field(description="The key of the provision: the descriptor name the constraint reads, matched verbatim as one flat key of the candidate object (any non-empty string, e.g. 'axes' or '@mikro/n_space_axes')")
    operator: DescriptorOperator = Field(description='The operator for the provision')
    value: Any | None = Field(default=None, description='The value of the provision. This can be any JSON serializable value; IN/NOT_IN take a list, LTE/GTE a number, EXISTS none')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class RegisterUiCatalogInput(BaseModel):
    """Register (upsert by name, scoped to the caller's organization) the components and operations a UI app can render and evaluate."""
    name: str = Field(description='The catalog name. Bloks and definitions reference it by this name; registering again replaces the previous components and operations.')
    description: str | None = Field(default=None, description='Human-readable description of the catalog.')
    components: Annotated[tuple[CatalogComponentInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The components this catalog can render.')
    'The components this catalog can render.\nDefault: []'
    operations: Annotated[tuple[CatalogOperationInput, ...] | None, GraphQLDefault('[]')] = Field(default=None, description='The pure operations this catalog can evaluate for UtilCalls.')
    'The pure operations this catalog can evaluate for UtilCalls.\nDefault: []'
    widget_defaults: Annotated[tuple['WidgetDefaultInput', ...] | None, GraphQLDefault('[]')] = Field(validation_alias=AliasChoices('widget_defaults', 'widgetDefaults'), serialization_alias='widgetDefaults', default=None, description='Default widgets per port kind and/or structure identifier. A UI renders them for ports that declare no widget; an identifier match beats a kind match. Each widget is validated against this catalog plus base at registration.')
    'Default widgets per port kind and/or structure identifier. A UI renders them for ports that declare no widget; an identifier match beats a kind match. Each widget is validated against this catalog plus base at registration.\nDefault: []'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class RequestMediaAccessInput(BaseModel):
    """No documentation"""
    store_id: str = Field(validation_alias=AliasChoices('store_id', 'storeId'), serialization_alias='storeId')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class RequestMediaUploadInput(BaseModel):
    """No documentation"""
    original_file_name: str = Field(validation_alias=AliasChoices('original_file_name', 'originalFileName'), serialization_alias='originalFileName')
    file_size: int | None = Field(validation_alias=AliasChoices('file_size', 'fileSize'), serialization_alias='fileSize', default=None)
    content_type: str | None = Field(validation_alias=AliasChoices('content_type', 'contentType'), serialization_alias='contentType', default=None)
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class RequiresInput(BaseModel):
    """No documentation"""
    key: str = Field(description="The key of the requirement: the descriptor name the constraint reads, matched verbatim as one flat key of the candidate object (any non-empty string, e.g. 'axes' or '@mikro/n_space_axes')")
    operator: DescriptorOperator = Field(description='The operator for the requirement')
    value: Any | None = Field(default=None, description='The value of the requirement. This can be any JSON serializable value; IN/NOT_IN take a list, LTE/GTE a number, EXISTS none')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ResolvedDependencyInput(BaseModel):
    """The input for mapping dependencies to implementations in a agent."""
    key: str = Field(description='The key of the dependency to map. This is used to identify the dependency in the system.')
    mapped_agents: tuple[MappedAgentInput, ...] = Field(validation_alias=AliasChoices('mapped_agents', 'mappedAgents'), serialization_alias='mappedAgents', description='The list of mapped agents to map to implementations in agents. This is used to identify the mapped agents in the system.')
    auto_resolve: Annotated[bool | None, GraphQLDefault('False')] = Field(validation_alias=AliasChoices('auto_resolve', 'autoResolve'), serialization_alias='autoResolve', default=None, description='Whether this dependency should be automatically resolved by the system. If true, the system will attempt to find a agent that can resolve this dependency and assign it to the action when the action is assigned. This is used to enable automatic resolution of dependencies without requiring the user to specify a specific agent for the dependency.')
    'Whether this dependency should be automatically resolved by the system. If true, the system will attempt to find a agent that can resolve this dependency and assign it to the action when the action is assigned. This is used to enable automatic resolution of dependencies without requiring the user to specify a specific agent for the dependency.\nDefault: False'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ResumeInput(BaseModel):
    """The input for resuming a task."""
    task: ID = Field(description='The task ID to resume')
    step: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Resume only until the next breakpoint instead of running on freely.')
    'Resume only until the next breakpoint instead of running on freely.\nDefault: False'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ReturnPortInput(PortTrait, BaseModel):
    """A Port is a single input or output of an action, identified by its `key` and typed by its `kind`.

    STRUCTURE, MEMORY_STRUCTURE and INTERFACE ports carry an `identifier` of the form `@package/key`
    (e.g. `@mikro/image`); ports with the same identifier are compatible. LIST and DICT ports have one
    child (the item type), UNION ports two or more (the variants), MODEL ports one per field. ENUM ports
    declare `choices`. See docs/design/ports.md for the full table.
    """
    key: str = Field(description="The key of the port: unique among its siblings, free of '..', not 'value'. LIST/DICT item ports are conventionally keyed '...'.")
    label: str | None = Field(default=None, description='The label of the port. This is the text that is displayed in the UI')
    kind: PortKind = Field(description='The kind of the port. This is the type of the port. Can be either int, string, structure, list, bool, dict, float, date, union or model')
    description: str | None = Field(default=None, description='The description of the port. This is the text that is displayed in the UI when the user hovers over the port')
    identifier: str | None = Field(default=None, description="The identifier of the port's type, of the form @package/key. Required for STRUCTURE, MEMORY_STRUCTURE and INTERFACE, where it is the only identity a value has; optional for MODEL and ENUM, where it names the class or enum the port was built from so that agents can map a value back to it.")
    nullable: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description='Whether the port is nullable or not. If the port is nullable, it can be set to null. If the port is not nullable, it cannot be set to null')
    'Whether the port is nullable or not. If the port is nullable, it can be set to null. If the port is not nullable, it cannot be set to null\nDefault: False'
    effects: tuple[EffectInput, ...] | None = Field(default=None, description='The effects of the port')
    choices: tuple[ChoiceInput, ...] | None = Field(default=None, description='The values the port accepts (required for ENUM; optional for INT, FLOAT, STRING). Rendered by CHOICE widgets.')
    reference_unit: str | None = Field(validation_alias=AliasChoices('reference_unit', 'referenceUnit'), serialization_alias='referenceUnit', default=None, description='For QUANTITY ports: the canonical/reference unit of the physical quantity, e.g. "volt" or "farad". It is the default selection and the key used to resolve the concrete quantity type; other units of the same dimension are still allowed.')
    proposed_units: tuple[str, ...] | None = Field(validation_alias=AliasChoices('proposed_units', 'proposedUnits'), serialization_alias='proposedUnits', default=None, description='For QUANTITY ports: units offered as a dropdown in the UI, e.g. ["pF", "nF", "uF"]. Proposals only — any unit of the same dimension remains valid input.')
    dimension: str | None = Field(default=None, description='For QUANTITY ports: the pint dimensionality string, e.g. "[mass] * [length] ** 2 / [time] ** 3 / [current]". This is the wiring-compatibility key between quantity ports.')
    children: tuple['ReturnPortInput', ...] | None = Field(default=None, description='The child ports (used for list, dict, union and model ports).')
    widget: 'ReturnWidgetInput | None' = Field(default=None, description='The return widget to use for this port, discriminated by `kind`.')
    provides: tuple[ProvidesInput, ...] | None = Field(default=None, description="The provisions for the port. Provisions are key-value pairs that can be used to add additional metadata to a port. When using rekuest's action search, you can filter actions based on their port provisions")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
ReturnWidgetInput = Annotated[ChoiceReturnWidgetInput | CustomReturnWidgetInput, Field(discriminator='kind')]

class ShelveInMemoryDrawerInput(BaseModel):
    """No documentation"""
    identifier: Identifier = Field(description='The identifier of the drawer. This is used to identify the drawer in the system.')
    resource_id: str = Field(validation_alias=AliasChoices('resource_id', 'resourceId'), serialization_alias='resourceId', description='The resource ID of the drawer.')
    label: str | None = Field(default=None, description='The label of the drawer. This is used to identify the drawer in the system.')
    description: str | None = Field(default=None, description='The description of the drawer. This is used to identify the drawer in the system.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ShortcutFilter(BaseModel):
    """No documentation"""
    and_: 'ShortcutFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ShortcutFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ShortcutFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    search: str | None = None
    ids: tuple[ID, ...] | None = None
    demands: tuple[PortDemandInput, ...] | None = None
    toolbox: ID | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ShortcutOrderName(BaseModel):
    """'name' variant of the @oneOf input 'ShortcutOrder'"""
    name: Ordering
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
ShortcutOrder = ShortcutOrderName

class StateAccessorInput(BaseModel):
    """No documentation"""
    option_key: OptionKey = Field(validation_alias=AliasChoices('option_key', 'optionKey'), serialization_alias='optionKey', description='The part of the state accessor to use as the value for the assign widget (e.g. the key, the description, the logo, etc.)')
    path: str | None = Field(default=None, description="Static JSON pointer into the state value ('/x/y'). Omit for the whole value. Mutually exclusive with `call`.")
    call: 'UtilCallInput | None' = Field(default=None, description="Pure UtilCall returning the pointer string dynamically. May reference `state`, `value` and the widget's `dependencies`. Mutually exclusive with `path`.")
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StateDefinitionInput(BaseModel):
    """A state schema is a blueprint for a state. It is composed of a definition, a list of dependencies, and a list of params."""
    ports: tuple[ReturnPortInput, ...] = Field(description='The ports of the state schema. This is used to define the structure of the state')
    name: str = Field(description='The name of the state schema. This is used to uniquely identify the state schema')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StateDemandInput(BaseModel):
    """Pure matching criteria for a state definition: hash short-circuits, port
    matches and protocols. Used directly by query filters and, wrapped in a
    StateDependencyInput, by dependency declarations."""
    hash: str | None = Field(default=None, description='The exact hash of the state definition. When set, matching short-circuits on the hash.')
    key: str | None = Field(default=None, description="The state's identity key on the agent (defaults to the interface at registration).")
    app: str | None = Field(default=None, description='The identifier of the app providing the state.')
    matches: tuple[PortMatchInput, ...] | None = Field(default=None, description="The matches the state definition's ports must satisfy.")
    protocols: tuple[str, ...] | None = Field(default=None, description='Protocols (by name) the state must implement.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StateDependencyInput(BaseModel):
    """A named state requirement of a dependency: a slot key plus the demand the
    agent's state must satisfy, and resolution-lifecycle filters."""
    key: str = Field(description='The local slot key of this state requirement — callers reference it when assigning.')
    description: str | None = Field(default=None, description='The description of the dependency, why it is needed and what it is used for.')
    demand: StateDemandInput | None = Field(default=None, description="The matching criteria the agent's state must satisfy (app/key preferred; matches loosen).")
    optional: Annotated[bool | None, GraphQLDefault('False')] = Field(default=None, description="Whether the dependency is optional or not. If the dependency is optional, the agent doesn't have to provide it to be potentially callable")
    "Whether the dependency is optional or not. If the dependency is optional, the agent doesn't have to provide it to be potentially callable\nDefault: False"
    allow_inactive: Annotated[bool | None, GraphQLDefault('True')] = Field(validation_alias=AliasChoices('allow_inactive', 'allowInactive'), serialization_alias='allowInactive', default=None, description='Allow inactive nodes, defaults to true')
    'Allow inactive nodes, defaults to true\nDefault: True'
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StateImplementationInput(BaseModel):
    """A state implementation is a blueprint for a state. It is composed of a definition, a list of dependencies, and a list of params."""
    interface: str = Field(description='The key of the state implementation. This is used to uniquely identify the state implementation')
    key: str | None = Field(default=None, description='The stable identity key of the state, matched by state demands. Defaults to the interface when omitted.')
    app: str | None = Field(default=None, description="The identifier of the app providing this state. Defaults to the registering agent's app identifier when omitted.")
    definition: StateDefinitionInput = Field(description='The schema of the state implementation. This is used to define the structure of the state')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class StrFilterLookup(BaseModel):
    """No documentation"""
    exact: str | None = None
    i_exact: str | None = Field(validation_alias=AliasChoices('i_exact', 'iExact'), serialization_alias='iExact', default=None)
    contains: str | None = None
    i_contains: str | None = Field(validation_alias=AliasChoices('i_contains', 'iContains'), serialization_alias='iContains', default=None)
    in_list: tuple[str, ...] | None = Field(validation_alias=AliasChoices('in_list', 'inList'), serialization_alias='inList', default=None)
    gt: str | None = None
    gte: str | None = None
    lt: str | None = None
    lte: str | None = None
    starts_with: str | None = Field(validation_alias=AliasChoices('starts_with', 'startsWith'), serialization_alias='startsWith', default=None)
    i_starts_with: str | None = Field(validation_alias=AliasChoices('i_starts_with', 'iStartsWith'), serialization_alias='iStartsWith', default=None)
    ends_with: str | None = Field(validation_alias=AliasChoices('ends_with', 'endsWith'), serialization_alias='endsWith', default=None)
    i_ends_with: str | None = Field(validation_alias=AliasChoices('i_ends_with', 'iEndsWith'), serialization_alias='iEndsWith', default=None)
    range: tuple[str, ...] | None = None
    is_null: bool | None = Field(validation_alias=AliasChoices('is_null', 'isNull'), serialization_alias='isNull', default=None)
    regex: str | None = None
    i_regex: str | None = Field(validation_alias=AliasChoices('i_regex', 'iRegex'), serialization_alias='iRegex', default=None)
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class TestTargetInput(BaseModel):
    """A test target: the action a test action tests, identified by exact hash or by an (app, key, version) coordinate. app defaults to the registering agent's app; omitting version matches every version."""
    hash: str | None = Field(default=None, description='The exact hash of the target action.')
    app: str | None = Field(default=None, description="The app identifier owning the target action. Defaults to the registering agent's app.")
    key: str | None = Field(default=None, description='The key of the target action. Matches every version unless version is given.')
    version: str | None = Field(default=None, description='Restrict a key target to one specific version.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ToolboxFilter(BaseModel):
    """No documentation"""
    name: StrFilterLookup | None = None
    and_: 'ToolboxFilter | None' = Field(validation_alias=AliasChoices('and_', 'AND'), serialization_alias='AND', default=None)
    or_: 'ToolboxFilter | None' = Field(validation_alias=AliasChoices('or_', 'OR'), serialization_alias='OR', default=None)
    not_: 'ToolboxFilter | None' = Field(validation_alias=AliasChoices('not_', 'NOT'), serialization_alias='NOT', default=None)
    distinct: bool | None = Field(validation_alias=AliasChoices('distinct', 'DISTINCT'), serialization_alias='DISTINCT', default=None)
    ids: tuple[ID, ...] | None = None
    search: str | None = None
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ToolboxOrderName(BaseModel):
    """'name' variant of the @oneOf input 'ToolboxOrder'"""
    name: Ordering
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
ToolboxOrder = ToolboxOrderName

class TrackInput(BaseModel):
    """A value that is being tracked over time during the runtime of an action. This is the state of a dependency"""
    dependency_key: str | None = Field(validation_alias=AliasChoices('dependency_key', 'dependencyKey'), serialization_alias='dependencyKey', default=None, description='The key of the dependency whose state is being tracked.')
    state_key: str = Field(validation_alias=AliasChoices('state_key', 'stateKey'), serialization_alias='stateKey', description='The key of the state to track.')
    value_key: str = Field(validation_alias=AliasChoices('value_key', 'valueKey'), serialization_alias='valueKey', description='The key of the value within the state to track.')
    label: str | None = Field(default=None, description='An optional human-readable label for the track.')
    description: str | None = Field(default=None, description='An optional description for the track.')
    windows: tuple['WindowInput', ...] | None = Field(default=None, description='The windows (aggregations) computed over the tracked value.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class UnshelveMemoryDrawerInput(BaseModel):
    """No documentation"""
    id: str = Field(description='The resource ID of the drawer.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class UtilCallInput(BaseModel):
    """Defines a utility call that can be invoked within the system."""
    operation: str = Field(description='The utility function name to invoke.')
    arguments: tuple[ActionArgumentInput, ...] | None = Field(default=None, description='Key-value arguments map compiled for the target utility call.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class ValidatorInput(ValidatorInputTrait, BaseModel):
    """
A validator for a port. `call` is a pure blok UtilCall evaluated client-side against the
catalog; it must return a boolean meaning 'valid'. Other ports the call references must be
listed in `dependencies` (the authoritative subscription list); `value` refers to the port's
own value. Use the .. syntax when traversing the tree of ports.
"""
    call: UtilCallInput = Field(description="The pure blok UtilCall, evaluated client-side against the catalog, that validates the port value. It must return a boolean meaning 'valid'. Argument value_paths may only reference names listed in `dependencies`, plus `value` for the port's own value.")
    dependencies: Annotated[tuple[str, ...] | None, GraphQLDefault('[]')] = Field(default=None, description="The form-field subscription list of the validator: the keys of the other ports whose values the call may reference. This list is authoritative: a value_path in the call may only reference these names (plus `value` for the port's own value). Use the .. syntax to traverse the tree of ports, e.g. 'foo..bar' for the child 'bar' of port 'foo'.")
    "The form-field subscription list of the validator: the keys of the other ports whose values the call may reference. This list is authoritative: a value_path in the call may only reference these names (plus `value` for the port's own value). Use the .. syntax to traverse the tree of ports, e.g. 'foo..bar' for the child 'bar' of port 'foo'.\nDefault: []"
    label: str | None = Field(default=None, description='An optional human-readable label for the validator.')
    error_message: str | None = Field(validation_alias=AliasChoices('error_message', 'errorMessage'), serialization_alias='errorMessage', default=None, description='The error message to display when the validation fails')
    source: str | None = Field(default=None, description='The authoring expression the call was compiled from (informational; never parsed or validated by the server).')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class WidgetDefaultInput(BaseModel):
    """A catalog's default widget for ports matching a kind and/or structure identifier. A UI applies it when a port has no explicit widget; an identifier match beats a kind match."""
    kind: PortKind | None = Field(default=None, description='Port kind the default applies to. With `identifier`, both must match.')
    identifier: str | None = Field(default=None, description="Structure identifier the default applies to, e.g. '@mikro/image'.")
    widget: AssignWidgetInput | None = Field(default=None, description='The assign widget to render for matching argument ports that declare no widget of their own.')
    return_widget: ReturnWidgetInput | None = Field(validation_alias=AliasChoices('return_widget', 'returnWidget'), serialization_alias='returnWidget', default=None, description='The return widget to render for matching return ports that declare no widget of their own.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)

class WindowInput(BaseModel):
    """A window that is calculated"""
    window_function: WindowFunction = Field(validation_alias=AliasChoices('window_function', 'windowFunction'), serialization_alias='windowFunction', description='The aggregation to compute over the tracked value within the window.')
    label: str | None = Field(default=None, description='An optional human-readable label for the window.')
    model_config = ConfigDict(frozen=True, extra='forbid', populate_by_name=True, use_enum_values=True)
ActionArgumentInput.model_rebuild()
ActionDemandInput.model_rebuild()
ActionFilter.model_rebuild()
AgentDependencyInput.model_rebuild()
AgentFilter.model_rebuild()
ArgPortInput.model_rebuild()
AssignInput.model_rebuild()
BlokImplementationInput.model_rebuild()
CatalogComponentInput.model_rebuild()
ComponentNodeInput.model_rebuild()
ComponentPropInput.model_rebuild()
CreateImplementationInput.model_rebuild()
CreateSpaceInput.model_rebuild()
CustomAssignWidgetInput.model_rebuild()
CustomReturnWidgetInput.model_rebuild()
DefinitionInput.model_rebuild()
EffectInput.model_rebuild()
ImplementAgentInput.model_rebuild()
ImplementationActionFilter.model_rebuild()
ImplementationAgentFilter.model_rebuild()
ImplementationFilter.model_rebuild()
ImplementationInput.model_rebuild()
OptimisticInput.model_rebuild()
PortDemandInput.model_rebuild()
PortMatchInput.model_rebuild()
RegisterUiCatalogInput.model_rebuild()
ReturnPortInput.model_rebuild()
SearchAssignWidgetInput.model_rebuild()
ShortcutFilter.model_rebuild()
StateAccessorInput.model_rebuild()
StateChoiceAssignWidgetInput.model_rebuild()
ToolboxFilter.model_rebuild()
TrackInput.model_rebuild()